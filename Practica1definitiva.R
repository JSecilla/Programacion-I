x = (media)
X = (3 5 6 8)

plot(2x, 4y)
nombre = "jesus
"
2x = "3, 4,5,6 "
Notas = "4, 5, 6, 7, 8,"
ricardo <- 4
jesus <- 5
ricardo + jesus 
durac = read.csv("4Duraciones.csv",dec=",",sep=";")
d.hist = durac %>% ggplot(aes(x=duracion)) +
  geom_histogram(aes(y=..density..),binwidth=0.1,colour="black",fill="steelblue") +
  
  pnorm(3,2.5,0.7071)

 help(hiper)
 pbinom(2,6,0.069)
 pgeom(2,0.158) - pgeom(1,0.158)
 Leire = "Guapa"
 NotadeIntroMates = 10
 Jesúsesde = "Córdoba"
 Jesúsenverdadesde = "Sevilla"
 NotadeIntroMates 
 mates1 = "10"
 mates2 + NotadeIntroMates
 mates2 = 10
 plot.default(2x,0,)
 plot(x = 4x, y = NULL, type = "p",  xlim = NULL, ylim = NULL,
      log = "", main = NULL, sub = NULL, xlab = NULL, ylab = NULL,
      ann = par("ann"), axes = TRUE, frame.plot = axes,
      panel.first = NULL, panel.last = NULL, asp = NA,
      xgap.axis = NA, ygap.axis = NA)
 
 
 
 if (check()) {
   x <- 1
   y <- 2
 } else {
   x <- 5
   y <- 10
 } 
 foo <- list(x=x, y=y)
 dpois (3,0.5)
 
 5 -> x
 print(x)